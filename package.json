{
  "name": "jarvis-ruben",
  "private": true,
  "packageManager": "npm@10.2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm install && npm install --prefix packages/backend && npm install --prefix packages/frontend",
    "dev": "npm run dev --prefix packages/frontend",
    "start": "npm run start --prefix packages/backend",
    "build": "npm run build --prefix packages/frontend",
    "build:all": "npm run build --prefix packages/frontend && npm run build --prefix packages/backend",
    "start:prod": "npm run build:all && npm run start --prefix packages/backend",
    "lint": "npm run lint --prefix packages/frontend"
  }
}
